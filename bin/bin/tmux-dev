#!/bin/sh

#tmux new-session -d $(basename $(pwd))
tmux new-session -d -n editor -s $1

tmux new-window -n extra
tmux new-window -n git
tmux new-window -n tests
tmux new-window -n servers
tmux attach -t $1
